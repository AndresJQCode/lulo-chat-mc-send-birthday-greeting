export enum TransacionStatus {
  PROCESSING = 'processing',
  APPROVED = 'APPROVED',
}

export enum OrderStatus {
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
  DECLINED = 'declined',
  PENDING = 'pending',
}
